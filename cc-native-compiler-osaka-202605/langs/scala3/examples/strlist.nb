function greet(name: string): string {
  return name;
}

val xs: list<int> = [1, 2, 3, 4, 5];
var total = 0;
var i = 0;
while (i < 5) {
  total = total + xs[i];
  i = i + 1;
}

print(greet("hello"));
print(total);
